export const FIELD_NAMES = {
  USERNAME: "username",
  EMAIL: "email",
  PASSWORD: "password",
  CHECKBOX: "checkbox"
};

export type RegisterFormValues = {
  username: string;
  email: string;
  password: string;
  checkbox: boolean;
};